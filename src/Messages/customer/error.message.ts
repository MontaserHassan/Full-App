enum errorCustomerMessage {
    server = 'Internal Server Error',
    invalidData = 'Invalid data',
    doesNotCreated = "Customer doesn't created",
    doesNotUpdated = "Customer doesn't updated",
    doesNotDeleted = "Customer doesn't deleted",
    emailExists = ' This is Email already exists',
    phoneNumberExists = ' This is Phone Number already exists',
    notFoundCustomer = 'This account does not exist',
    wrongPassword = 'This Password is wrong',
    notUpdated = "Customer's data doesn't update"
};



export default errorCustomerMessage;